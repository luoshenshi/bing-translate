{
  "name": "translator",
  "version": "1.0.0",
  "description": "just a translator using an API (bing api)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "leoLix",
  "license": "ISC",
  "dependencies": {
    "bing-translate-api": "^2.10.0",
    "express": "^4.18.2"
  }
}
